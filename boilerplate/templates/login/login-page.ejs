/**
* This page is a login page, where this page is always used when the user will use the application.
*/

import React, { Component } from 'react';
import PropTypes from 'prop-types';
import { connect } from 'react-redux';
import <%=func.camelCase(loginComponentFileName, {pascalCase: true})%> from './../../modules/<%=module%>/component/<%=func._.kebabCase(loginComponentFileName)%>';
import { authenticate } from './../../modules/<%=module%>/store/login-action';

export class <%=func.camelCase(loginFileName, {pascalCase: true})%> extends Component {
  static propTypes = {
    prop: PropTypes
  }

  render() {
    return (
      <React.Fragment>
          <<%=func.camelCase(loginComponentFileName, {pascalCase: true})%> {...this.props}/>
      </React.Fragment>
    )
  }
}

const mapStateToProps = (state) => ({
  ...state.<%=loginReducerKey%>
})

const mapActionToProps = () => ({
    onSubmit : authenticate, 
});

export default connect(mapStateToProps, mapActionToProps())(<%=func.camelCase(loginFileName, {pascalCase: true})%>)
